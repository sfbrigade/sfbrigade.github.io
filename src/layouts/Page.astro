---
import { base } from "@/utils/urls";
import "@picocss/pico";
import "./global.css";

interface Props {
	title: string;
}

const { title } = Astro.props;
const baseURL = import.meta.env.BASE_URL.replace(/([^/])$/, "$1/");
---

<!doctype html>
<html lang="en" data-theme="light">
	<head>
		<base href={baseURL} />
		<meta charset="UTF-8" />
		<meta name="description" content="The home of SF Civic Tech" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/svg+xml" href={base("favicon.ico")} />

		<title>{title} | SF Civic Tech</title>
	</head>
	<body>
		<slot />
	</body>
</html>
